{
  "name": "modo-fe",
  "version": "0.1.0",
  "description": "Frontend Modo Project using Monorepo Architecture",
  "scripts": {
    "dev": "run-p \"dev:*\"",
    "dev:css": "npm run generate:css -- --watch",
    "dev:mfa": "npm run mfa",
    "dev:main": "next dev",
    "build": "npm run generate:css && tsc && next build",
    "preview": "next start -p 5173",
    "host": "run-p \"host:*\"",
    "host:mfa": "npm run mfa",
    "host:css": "npm run generate:css -- --watch",
    "host:dev": "npm run dev -workspace=@workspace/host",
    "auth": "run-p \"auth:*\"",
    "auth:css": "npm run generate:css -- --watch",
    "auth:dev": "npm run dev -workspace=@workspace/auth",
    "books": "run-p \"books:*\"",
    "books:css": "npm run generate:css -- --watch",
    "books:dev": "npm run dev -workspace=@workspace/books",
    "mfa": "run-p \"mfa:*\"",
    "mfa:auth": "npm run build -workspace=@workspace/auth && npm run preview -workspace=@workspace/auth",
    "mfa:books": "npm run build -workspace=@workspace/books && npm run preview -workspace=@workspace/books",
    "aws": "dotenv -e .env -- cross-var ssh %SSH_HOST%",
    "lint": "next lint && tsc",
    "test": "jest",
    "generate:css": "npx tailwindcss -i ./packages/styles/global.css -o ./packages/styles/tailwind-LEGACY.css",
    "prepare": "husky install",
    "lint-staged": "lint-staged"
  },
  "lint-staged": {
    "{src,packages}/**/*.{js,jsx,ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "micro_services/Auth/**/*.{js,jsx,ts,tsx}": [
      "eslint --fix --config micro_services/Auth/.eslintrc.js",
      "prettier --write"
    ],
    "micro_services/Books/**/*.{js,jsx,ts,tsx}": [
      "eslint --fix --config micro_services/Books/.eslintrc.js",
      "prettier --write"
    ],
    "micro_services/Host/**/*.{js,jsx,ts,tsx}": [
      "eslint --fix --config micro_services/Host/.eslintrc.js",
      "prettier --write"
    ]
  },
  "workspaces": [
    "micro_services/*",
    "packages/**/*"
  ],
  "dependencies": {
    "@packages/config": "^0.1.0",
    "npm-run-all": "^4.1.5",
    "react-hook-form": "^7.43.1",
    "sass": "^1.63.6"
  },
  "devDependencies": {
    "@packages/assets": "^0.1.0",
    "@packages/components": "^0.1.0",
    "@packages/config": "^0.1.0",
    "@packages/config-next": "^0.1.0",
    "@packages/styles": "^0.1.0",
    "@packages/tsconfig": "^0.1.0",
    "eslint-config-preset": "^0.1.0"
  }
}
